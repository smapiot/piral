import { createState, destroyState, replaceState } from './actions';

describe('Piral-Containers actions module', () => {
  it('creates a substate if desired', () => {

  });

  it('destroys the substate if desired', () => {

  });

  it('updates the substate if desired', () => {

  });
});
