import { useDynamicLanguage, useTranslate } from './hooks';

describe('Piral-Translate hooks module', () => {
  it('translates from the given translations', () => {

  });

  it('switches to a remote language', () => {

  });
});
