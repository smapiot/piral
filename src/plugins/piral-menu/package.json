{
  "name": "piral-menu",
  "version": "1.9.1",
  "description": "Plugin for the integration of menu items in Piral.",
  "keywords": [
    "piral",
    "pilet-api",
    "smapiot",
    "portal",
    "modules",
    "api",
    "plugin",
    "plugin-component",
    "menu"
  ],
  "author": "smapiot",
  "homepage": "https://piral.io",
  "license": "MIT",
  "module": "lib/index.js",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "exports": {
    ".": "./lib/index.js",
    "./lib/*": {
      "types": "./lib/*.d.ts",
      "import": "./lib/*"
    },
    "./package.json": "./package.json"
  },
  "sideEffects": false,
  "files": [
    "lib",
    "src",
    "piral-menu.min.js"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/smapiot/piral.git"
  },
  "bugs": {
    "url": "https://github.com/smapiot/piral/issues"
  },
  "scripts": {
    "cleanup": "rimraf esm lib piral-menu.min.js",
    "build": "yarn build:bundle && yarn build:esnext",
    "build:bundle": "esbuild src/index.ts --outfile=piral-menu.min.js --bundle --external:piral-core --external:react --minify --global-name=piralMenu",
    "build:esnext": "tsc --project tsconfig.json --outDir lib --module esnext",
    "typedoc": "typedoc --json ../../../docs/types/piral-menu.json src --exclude \"src/**/*.test.*\"",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "piral-core": "^1.9.1",
    "react": "^18.0.0"
  }
}
