/**
 * @vitest-environment jsdom
 */
import { describe, it } from 'vitest';
import { gqlQuery } from './queries';

describe('Piral-Urql queries module', () => {
  it('runs a successful query', () => {});
});
