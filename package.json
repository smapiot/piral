{
  "name": "piral",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna clean && lerna bootstrap --hoist",
    "init": "npm run bootstrap && lerna run build && npm run bootstrap",
    "test": "jest",
    "publish": "lerna run build && lerna publish"
  },
  "devDependencies": {
    "@types/jest": "^24.0.5",
    "jest": "^24.1.0",
    "jest-cli": "^24.1.0",
    "lerna": "^3.10.7",
    "prettier": "^1.16.4",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^3.3.1"
  }
}
